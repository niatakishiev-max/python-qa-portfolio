# Python / QA Portfolio

Учебное портфолио начинающего Python-разработчика и QA Automation trainee.

Цель портфолио — показать практические навыки:

- Python basics;
- работа с консольными приложениями;
- функции и разделение бизнес-логики;
- JSON-хранение данных;
- unit-тесты на `pytest`;
- параметризация тестов;
- fixtures в `pytest`;
- тестирование пользовательского ввода через `monkeypatch`;
- проверка вывода через `capsys`;
- временные файлы в тестах через `tmp_path`;
- тестирование сохранения и загрузки JSON;
- GitHub Actions / CI;
- HTTP и базовое API-тестирование;
- библиотека `requests`;
- ручное тестирование;
- тест-кейсы, чеклисты и баг-репорты;
- Git и GitHub.

## Python Projects

### Python ToDo CLI

Репозиторий: https://github.com/niatakishiev-max/python-todo-cli

Консольный менеджер задач.

Что реализовано:

- добавление задач;
- удаление задач;
- перенос задач в выполненные;
- возврат выполненных задач;
- сохранение данных в JSON;
- напоминания по задачам;
- обработка некорректного ввода;
- разделение кода на функции;
- unit-тесты на `pytest`;
- тестирование JSON через `tmp_path`;
- документация по тестированию;
- GitHub Actions для автоматического запуска тестов.

Что показывает проект:

- списки и словари;
- работа с JSON;
- функции;
- консольное меню;
- базовая архитектура CLI-приложения;
- выделение бизнес-логики из интерфейса;
- тестирование сохранения и загрузки данных;
- базовое понимание CI/CD через GitHub Actions.

### Python Calculator CLI

Репозиторий: https://github.com/niatakishiev-max/python-calculator-cli

Консольный калькулятор с автотестами.

Что реализовано:

- сложение;
- вычитание;
- умножение;
- деление;
- защита от деления на ноль;
- проверка неправильного ввода;
- выход через `q`;
- unit-тесты на `pytest`.

Что показывает проект:

- чистые функции;
- разделение логики и интерфейса;
- `pytest`;
- `pytest.mark.parametrize`;
- `monkeypatch`;
- `capsys`;
- `requirements.txt`;
- README с инструкциями запуска и тестирования;
- GitHub Actions для автоматического запуска тестов.

### Python Guess Number CLI

Репозиторий: https://github.com/niatakishiev-max/python-guess-number-cli

Консольная игра “Угадай число”.

Что реализовано:

- генерация случайного числа;
- уровни сложности;
- ограничение попыток;
- подсказки больше/меньше;
- повтор игры;
- обработка некорректного ввода.

Что показывает проект:

- циклы;
- условия;
- функции;
- работа с модулем `random`;
- базовая игровая логика.

### Python Sales Report CLI

Репозиторий: https://github.com/niatakishiev-max/python-sales-report-cli

Консольная программа для учёта продаж и формирования бизнес-отчётов.

Что реализовано:

- добавление продаж;
- просмотр продаж;
- удаление продаж;
- сохранение данных в JSON;
- загрузка данных из JSON;
- расчёт выручки;
- расчёт общего количества товаров;
- группировка отчёта по товарам;
- проверка некорректного ввода;
- unit-тесты на `pytest`;
- `pytest.mark.parametrize`;
- fixtures для переиспользуемых тестовых данных;
- тестирование ввода через `monkeypatch`;
- проверка вывода через `capsys`;
- тестирование файлового хранения через `tmp_path`;
- GitHub Actions для автоматического запуска тестов;
- документация по тестированию.

Что показывает проект:

- бизнес-логика;
- списки и словари;
- группировка данных;
- JSON-хранение;
- вынесение расчётов в отдельные функции;
- unit-тестирование бизнес-логики;
- тестирование пользовательского ввода;
- тестирование чтения и записи JSON без затрагивания реального файла данных;
- базовое понимание CI/CD через GitHub Actions.

### Python API Testing Practice

Репозиторий: https://github.com/niatakishiev-max/python-api-testing-practice

Учебный проект для практики HTTP и API-тестирования на Python.

Что реализовано:

- первый API-клиент через `requests`;
- GET-запрос к публичному API;
- POST-запрос с JSON body;
- PUT-запрос для полного обновления ресурса;
- PATCH-запрос для частичного обновления ресурса;
- DELETE-запрос для удаления ресурса;
- проверки status code;
- проверки структуры JSON-ответа;
- проверка `404 Not Found`;
- автотесты API через `pytest`;
- GitHub Actions для автоматического запуска API-тестов.

Что показывает проект:

- понимание request / response;
- базовые HTTP-методы `GET`, `POST`, `PUT`, `PATCH`, `DELETE`;
- работу с JSON API;
- проверку status code;
- проверку структуры ответа;
- базовый уровень API automation.

## QA Projects

### QA ToDo CLI Testing

Репозиторий: https://github.com/niatakishiev-max/qa-todo-cli-testing

QA-документация для проекта `python-todo-cli`.

Что есть в проекте:

- требования;
- чеклист;
- тест-кейсы;
- тестовый прогон;
- баг-репорты.

Что показывает проект:

- понимание ручного тестирования;
- умение переводить требования в проверки;
- оформление тестовой документации;
- фиксация результатов тестирования.

### QA Sales Report CLI Testing

Репозиторий: https://github.com/niatakishiev-max/qa-sales-report-cli-testing

QA-документация для проекта `python-sales-report-cli`.

Что есть в проекте:

- требования;
- чеклист;
- тест-кейсы;
- ручной тестовый прогон;
- баг-репорты;
- связь с тестируемым Python-проектом.

Что показывает проект:

- тестирование бизнес-сценариев;
- проверка валидации ввода;
- проверка отчётов;
- проверка сохранения данных;
- оформление QA-артефактов.

## Current Skill Level

На текущем этапе портфолио показывает уверенную базу для дальнейшего развития в сторону:

- Junior Python Developer;
- Junior QA Engineer;
- Junior QA Automation Engineer.

Уже освоено на практике:

- написание небольших CLI-приложений;
- Git/GitHub workflow;
- базовая декомпозиция кода на функции;
- выделение бизнес-логики из консольного интерфейса;
- ручное тестирование;
- оформление QA-документации;
- unit-тестирование на `pytest`;
- параметризация тестов;
- fixtures;
- `monkeypatch` и `capsys`;
- `tmp_path` для временных файлов;
- тестирование JSON-сохранения и загрузки;
- базовый CI через GitHub Actions.

Следующие шаги развития:

- добавить GitHub Actions в остальные проекты с тестами;
- добавить автотесты для ToDo CLI;
- углубить fixtures и тестирование файлов;
- углубить HTTP и API;
- углубить API-тестирование: headers, query params, negative cases;
- изучить SQL на базовом уровне;
- сделать небольшой Flask/FastAPI проект;
- добавить тесты API и CI для финального проекта.
